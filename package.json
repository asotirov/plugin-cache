{
  "name": "cache",
  "version": "0.1.0",
  "main": "cache.js",
  "dependencies": {
    "cache-manager": "^2.9.0",
    "cache-manager-redis": "^0.6.0"
  },
  "plugin": {
    "consumes": [
      "options.cache"
    ],
    "provides": [
      "cache"
    ]
  }
}
